import {ResetIcon} from '@radix-ui/react-icons' 

const ResetButton = () => {
  return (
    <button className="reset-btn">
        <ResetIcon></ResetIcon>
    </button>
  )
}

export default ResetButton